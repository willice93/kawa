Click Open.
e In the main VeraCrypt window, click Mount. Type the password.

e VeraCrypt will now mount the volume. We have just successfully mounted the container
as a virtual disk. The virtual disk is entirely encrypted (including file names, allocation
tables, free space, etc.) and behaves like a real disk. You can save (or copy, move, etc.)
files to this virtual disk and they will be encrypted as they are being written. You can
copy files (or folders) to and from the VeraCrypt volume just as