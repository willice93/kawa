You will then be prompted with street_address (TEXT). In this example, I will enter 1066 W Addison St, Chicago, IL 60613. This stores the entered location into our current workspace. The following commands load the geocode module which will convert our address to GPS coordinates; execute the module; display and confirm our locations; load the Twitter “pushpin” module; execute the script; and display the Twitter posts that were geo-tagged near our location. use recon/locations-locations/geocode