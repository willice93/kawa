by -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/niaster/install)" After Brew is installed, type the following commands, hitting “Return” after each line, into the same Terminal application used previously. brew install clamav cd /usr/local/etc/clamav/ cp freshclam.conf.sample freshclam.conf sed -ie 's/AExample/#Example/g’ freshclam.conf These steps will install ClamAV, switch to the installation directory, make a copy of the configuration file, and then modify the configur