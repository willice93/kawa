at. Type the following text into the document and save it. set /p VIDEO=Enter full name of video file on desktop: md “%userprofile%\desktop\ frames” ffrnpeg.exe -y -i “%userprofile%\desktop\%VIDEO%” -an -r 10 “%userprofile%\desktop\frames\img%%3d.bmp” It is very important that these files do not have a “txt” extension. If you double click this file and it opens within Notepad, the file extension is “txt” and not “bat”. Change the file extension and you should see a black box open. In Figure 19.0