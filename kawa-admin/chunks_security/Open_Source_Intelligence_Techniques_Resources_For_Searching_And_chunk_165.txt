ture utility within Firefox, there are some instances where it does not perform well. If you have a target’s Facebook page that has a lot of activity present, this may create a screen capture too large for FireShot The rendering process will likely expend all of the computer’s video memory and fail to create the file. When this happens, I use Nimbus as my first backup. Nimbus allows you to specify whether you want to capture only the visible portion of the page, the entire page, or a custom sele