d be played universally on any mobile platform. During the class, I created a batch file that used FFmpeg to extract die audio from any video file. Enter the following into a text file and save it as audio.bat within the same video folder created earlier. This script will extract the audio track from the supplied video and save it as a 320k MP3 file on the Desktop. The file name will be identical to the video file name with MP3 added to the end. set /p VIDEO=Video file name (with extension) on D